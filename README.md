# Recall

Recall is a production-grade RAG system that retrieves over large document corpora using hybrid search and cross-encoder reranking, then generates answers with inline citations traceable to source chunks.

Every pipeline change is measured against a golden eval set — tracking context recall, context precision, and answer faithfulness — making retrieval failure visible and fixable.
